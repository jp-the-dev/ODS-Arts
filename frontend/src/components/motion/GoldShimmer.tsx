'use client'
// Subtle shimmer on gold elements. CSS keyframe via motion. Props: children
// Client Component — required for Framer Motion
// Respects prefers-reduced-motion via useReducedMotion()
export default function GoldShimmer() {
  return <></>
}
