'use client'
// Observes scroll past hero and applies filled-nav class. No visual output
// Client Component — required for Framer Motion
// Respects prefers-reduced-motion via useReducedMotion()
export default function NavigationScrollObserver() {
  return <></>
}
