'use client'
// Border draw + subtle scale on hover. whileHover. Props: children, className
// Client Component — required for Framer Motion
// Respects prefers-reduced-motion via useReducedMotion()
export default function CardHoverEffect() {
  return <></>
}
