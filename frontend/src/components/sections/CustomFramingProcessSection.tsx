// Custom framing process — 4-step timeline. Ref: wireframe §09
// Server Component shell — passes data to Client motion wrappers as children/props
export default function CustomFramingProcessSection() {
  return <section />
}
