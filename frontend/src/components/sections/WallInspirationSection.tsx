// Wall inspiration — mosaic/marquee, style filters. Ref: wireframe §08
// Server Component shell — passes data to Client motion wrappers as children/props
export default function WallInspirationSection() {
  return <section />
}
