// Gifting banner — full-bleed image, dual CTA. Ref: wireframe §11
// Server Component shell — passes data to Client motion wrappers as children/props
export default function GiftingBannerSection() {
  return <section />
}
