// Customer stories — masonry grid, real homes. Ref: wireframe §06
// Server Component shell — passes data to Client motion wrappers as children/props
export default function CustomerStoriesSection() {
  return <section />
}
