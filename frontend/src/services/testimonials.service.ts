// Testimonial and customer story data — Server only
import type { Testimonial, CustomerStory, InspirationImage } from '@/types'

export async function getTestimonials(): Promise<Testimonial[]> {
  return []
}

export async function getCustomerStories(): Promise<CustomerStory[]> {
  return []
}

export async function getInspirationImages(): Promise<InspirationImage[]> {
  return []
}
